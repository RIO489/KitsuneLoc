# Оновлення програми перекладу до версії 1.3

Твій переклад не постраждає. Він живе в книгах `.xlsx` у теці `Переклад\`,
а в архіві лише скрипти й **одна нова книга** (про неї нижче).

## Що робити

1. Закрий усі книги в Excel і вікно «Переклад», якщо воно відкрите.
2. Розпакуй цей архів **поверх своєї теки `trans`**, з заміною файлів.
   Архів не перезапише жодної твоєї книги, а також `work\`, `backup\`
   і `settings.json`. Він тільки додає нову книгу
   `Переклад\Mary Skelter\24 Написи на картинках.xlsx`.
3. **Двічі клацни `Встановити.bat`.** Цього разу обов'язково: для
   написів на картинках потрібні нові бібліотеки (Pillow, texture2ddecoder,
   etcpak). Якщо забудеш, програма сама запропонує їх доставити.
4. Запусти `Переклад.pyw`, обери Mary Skelter, натисни
   **«1. Дістати текст з гри»**. У лозі мають бути рядки
   «підхоплено перекладів: N» і «написи на картинках: 319».
5. Далі як завжди: перекладаєш → **«2. Залити переклад у гру»** →
   **«3. Запустити гру»**.

## Що нового у версії 1.3

- **Написи на картинках.** Кнопки й заголовки, намальовані в текстурах
  гри (`Start`, `YES / NO`, `Skip`, меню табору, налаштування, бойові
  команди, результати бою, бібліотека, карта районів, бонуси…),
  тепер перекладаються як звичайні рядки: у книзі
  **«24 Написи на картинках»**. Програма сама стирає англійський напис і
  малює твій схожим шрифтом, у тих самих кольорах. Кнопка має фіксований
  розмір, тож задовге слово буде дрібнішим.
- **Вікно з прев'ю.** Кнопка **«Написи на картинках (з прев'ю)…»**:
  пишеш переклад, і поруч одразу видно, як кнопка виглядатиме в грі.
  Там же видно, чи напис вміщається.
- **Сюрприз.** Книга «24 Написи на картинках» у цьому архіві **вже
  частково перекладена** — меню, кнопки, бій. Переглянь і поправ на свій
  смак, решту доперекладай.
- Програма при запуску перевіряє всі потрібні бібліотеки, а не лише одну.

Докладніше — у `ЧИТАТИ ПЕРЕКЛАДАЧУ.md`, розділ «Написи на картинках».

## Одне обмеження (як і раніше)

У звичайному тексті **не вживай** символи `ì Ì ò Ò ù Ù ã Ã`: ці слоти
шрифту гри зайняті під `і І ї Ї є Є ґ Ґ`. У книзі «24 Написи на
картинках» обмеження немає, пиши звичайні `і ї є ґ`.

## Якщо щось пішло не так

Кнопка **«Повернути оригінали гри»** повертає гру до стану до перекладу.
