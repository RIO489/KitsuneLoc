# KitsuneLoc — як перекладати

## Один раз на початку

1. Постав Python: https://www.python.org/downloads/ — під час встановлення
   обов'язково постав галочку **«Add Python to PATH»**.
2. Відкрий теку `trans`, двічі клацни `Встановити.bat`. Воно доставить
   потрібні бібліотеки. Одна хвилина, робиться раз.

## Щодня

Двічі клацни **`Переклад.pyw`** — відкриється вікно.

1. Вгорі обери гру.
2. **Лише для Crystar:** обери «Мова тексту в грі» — англійська чи японська.
   Це та мова, яку ти поставиш у налаштуваннях самої гри. Переклад піде
   саме туди, замість неї. Якщо в грі стоїть японська, а тут англійська —
   у грі перекладу не побачиш.
3. Перевір, чи правильно вказано теку гри. Якщо ні — «Обрати…».
4. **«1. Дістати текст з гри»** — програма сама витягне всі репліки й збере
   файли Excel. Перший раз для Crystar це кілька хвилин (840 сцен), для
   Mary Skelter — швидше. Прогрес видно внизу.
5. **«Відкрити теку з перекладом»** — там лежать книги `.xlsx`.
6. Перекладаєш. Зберігаєш файл (звичайний Ctrl+S).
7. **«2. Залити переклад у гру»** — збере файли гри й сам підставить їх
   у теку гри. Оригінали при цьому копіюються в `backup`.
8. **«3. Запустити гру»** — дивишся, чи все на місці.

Повторюєш пункти 6–8 скільки завгодно разів. Кнопка «1» теж безпечна: вона
не стирає перекладене, лише підтягує нові рядки з гри.

Якщо щось пішло не так — **«Повернути оригінали гри»** поверне все як було.

## Що є у вікні

Зверху — вибір гри й тека з нею. Кнопка **«Знайти»** сама шукає гру в усіх
бібліотеках Steam, вручну вказувати шлях треба тільки якщо не знайшла.
Праворуч видно, що зараз лежить у грі — переклад чи оригінал.

Посередині три кроки, поруч — список книг і **«Відкрити книгу»**, щоб не
лазити в теку. Унизу — стан, час роботи й кнопка **«Зупинити»**: довгі
операції можна перервати, у теці гри при цьому нічого не зміниться.

Праворуч внизу — **«Темна тема»** і **«Скопіювати лог»** (усе, що програма
пише, також лягає у файл `лог.txt` поруч з програмою — його зручно
переслати, якщо щось пішло не так).

## Скільки вже зроблено

Кнопка **«Мій прогрес»** рахує все прямо з твоїх книг і показує відсоток —
і загальний, і по кожній книзі окремо, зі смужками. Там же видно, скільки
рядків додалося з минулого разу, і скільки з перекладеного програма
підставила автоматично (їх варто переглянути очима).

Рахунок іде по збережених книгах, тож перед натисканням тисни Ctrl+S.

## Які книги є

Одна книга — один розділ гри, у порядку проходження (номер на початку
назви задає порядок у теці).

**Crystar:** «00 Меню та інтерфейс», «01 Пролог», «02 Розділ 1» … «08 Розділ 7»,
«09 Фінальний розділ», «10–12 Повторний прохід», «13 Дзвінки»,
«14 Персонажі, NPC, магазин».

**Mary Skelter:** «00 Імена», «01 Інтерфейс», «02–13 Сюжет …» (групи сцен у
порядку гри), «14–15 Додаткові сцени», «16 Короткі репліки», «17–22» таблиці
(спорядження, навички, завдання, локації…), «23 Описи DLC»,
**«24 Написи на картинках»** — див. окремий розділ нижче.

**Neptunia Re;Birth1:** «01 Меню та система», «02 Предмети», «03 Створення
(Remake)», «04 Навички та вміння», «05 Монстри», «06 Персонажі», «07 Завдання
та локації», «08 Довідка, галерея, інше», «10–19 Сцени 00xx…09xx» (сюжет,
за номером сцени), «20 Інші сцени», «21 DLC». Уже зроблений раніше переклад
(близько 10 900 рядків) перенесено в книги — його видно одразу.

Якщо книга зникла з теки — не лякайся: при зміні розкладки старі книги
переносяться в підтеку `_старі книги`, а все перекладене вже є в нових.

## Що в книзі Excel

| Стан | Сцена | Хто / ключ | Оригінал (EN) | Японська | Переклад | Примітка | Підказка |
|---|---|---|---|---|---|---|---|
| рахується сам | номер сцени | хто говорить | англійський рядок | японський рядок | **сюди пишеш** | твої нотатки | пише програма |

Колонка «Японська» є в Crystar і в сюжеті та іменах Mary Skelter (японський
оригінал узято з японського видання гри). В «Інтерфейсі» Mary Skelter її
немає: там японська й англійська таблиці влаштовані по-різному, і програма
могла б поставити поруч не той рядок.

**«Стан»** оновлюється сам, щойно ти щось пишеш:

- `⬜ не перекладено` — «Переклад» порожній;
- `✅ готово` — переклад є;
- `🔵 перевір` — програма сама підставила переклад, бо **точно такий самий**
  англійський рядок ти вже переклав деінде. Такі рядки **повністю
  підсвічені блакитним**. Їх варто переглянути: той самий англійський рядок
  у різних місцях може перекладатись по-різному.

Як знайти всі блакитні рядки: у заголовку колонки «Стан» натисни стрілочку
фільтра → лиши галочку тільки на `🔵 перевір` → OK. Так само можна
відфільтрувати `⬜ не перекладено`, щоб бачити, що лишилось.

Якщо переглянув блакитний рядок і переклад підходить — напиши будь-що в
«Примітку» (наприклад «ок»), і стан стане `✅ готово`. Якщо змінив переклад,
він теж стане `✅ готово` сам.

«Примітка» — твоя: пиши що завгодно, вона зберігається між запусками.
«Підказка» — сіра колонка, яку заповнює програма (що означають теги й коди).

Переклад один на обидві мови. Перекладати можна з англійської або з
японської, а куди саме він піде в грі, вирішує перемикач у вікні.

**Книга «Імена»** (Mary Skelter) — усі імена мовців гри, кожне **один раз**.
Переклав тут «Jack» → «Джек», і воно підставиться в усі репліки Джека в
усіх діалогах. В інших книгах імен немає, тож плутанини не буде.

**Службове** — внутрішні ключі гри (`EV_CHARA_0026`, `CONSUME_001`). Їх
**не чіпати**, вони там лише для довідки.

## Коди в інтерфейсі (Mary Skelter)

У книзі «Інтерфейс» трапляються службові вставки. Їх треба перенести в
переклад **як є**, на потрібне місце в реченні:

- `%s`, `%d`, `%2d`, `%+d`… — сюди гра підставить назву чи число.
  Приклад: `Got %s.` → `Отримано %s.`
- `#I`, `#F` — службові коди.

Якщо загубити такий код, у грі зникне число чи назва. Якщо дописати
**зайвий** `%s`, гра може впасти. «Перевірити переклад» ловить обидва випадки.

## Теги в тексті (Crystar)

Інколи в оригіналі трапляються вставки в кутових дужках. Гра замінює їх
на льоту. Що з ними робити, підкаже колонка «Підказка», коротко так:

| тег | що це | що робити |
|---|---|---|
| `<ITEM>` `<VALUE>` `<SKILL>` `<TIME>` `<TYPE>` `<CHARA>` | гра підставить назву предмета, число, ім'я… | **перенести в переклад як є**, на потрібне місце в реченні |
| `<ICON=…>` `<BUTTON=…>` | значок або кнопка геймпада | **перенести як є** |
| `<#FA8072>текст</color>` | колір тексту | **лишити обидва теги** навколо перекладу |
| `<CHARA=…>` `<WORD=…>` `<SYS=…>` | ім'я чи термін з довідника гри | можна написати словом, а можна лишити тег |

Приклад: `<ITEM> used <VALUE> remaining` → `Використано <ITEM>, лишилось <VALUE>`.
Якщо `<VALUE>` загубити, число просто зникне з екрана.

У колонці «Японська» імена й терміни вже розшифровані: замість
`<CHARA=PLAYER_0001>` там написано `〔零〕`. Це лише для зручності читання.

Кнопка «Перевірити переклад» покаже кожен рядок, де загублено тег.

## Переноси рядків

У грі вікно репліки вузьке, і перенос рядка зашитий прямо в текст. В Excel
він виглядає як розрив усередині клітинки (Alt+Enter).

**Скільки рядків було в оригіналі — стільки має лишитись у перекладі**,
і кожен рядок не має бути значно довшим за англійський. Інакше текст вилізе
за вікно. Кнопка **«Перевірити переклад»** покаже всі місця, де це порушено.

## Українські літери у грі (Mary Skelter)

Пиши звичайну українську — `і ї є ґ` і всі решта. Раніше гра мовчки
викидала ці літери (`Мрія` ставала `Мря`), бо в її шрифті їх просто немає.
Тепер програма при заливанні перебудовує шрифти гри й домальовує їх, тож
усе видно як має бути.

Єдине: **не вживай символи `ì Ì ò Ò ù Ù ã Ã`** — ці вісім слотів у шрифті
тепер зайняті під українські літери, і в грі замість них зʼявляться
`і І ї Ї є Є ґ Ґ`. Кнопка «Перевірити переклад» попередить, якщо вони десь
трапляться.

## Написи на картинках (Mary Skelter)

Частина написів у грі — не текст, а картинка: кнопки `Start`, `YES / NO`,
`Skip`, пункти меню табору, бойові команди, заголовки `MENU`, `PARTY`,
назви районів на карті тощо. Вони зібрані в окрему книгу
**«24 Написи на картинках»**. Програма сама стирає англійський напис і малює
твій переклад схожим шрифтом, у тих самих кольорах, з обведенням і сяйвом.

- Однаковий напис (наприклад, `Attack` чи ім'я героїні) — **один рядок**,
  переклад піде в усі місця, де він трапляється.
- Порожня клітинка — у грі лишиться оригінальна картинка.
- Регістр пишеш сам: як напишеш `ТАК` чи `Так`, так і буде.
- Тут **можна** писати `і ї є ґ` — ці написи малює програма, обмеження
  шрифту гри на них не діють.
- Кнопка має фіксований розмір. Довгий переклад програма стисне й зменшить;
  якщо він удвічі довший за оригінал, стане помітно дрібнішим — краще
  пошукати коротше слово.

### Вікно з прев'ю

У головному вікні є кнопка **«Написи на картинках (з прев'ю)…»**. Ліворуч
список написів, праворуч поле перекладу, а під ним — **як кнопка виглядатиме
в грі**: оригінал і результат, усі варіанти (звичайна й вибрана). Картинка
оновлюється, щойно перестаєш друкувати. Під полем видно, чи напис вміщається.

`Enter` або `↓` — наступний напис, `↑` — попередній. **«Зберегти в книгу»**
записує в ту саму книгу Excel, далі як завжди — кнопка «2». Перед збереженням
закрий цю книгу в Excel, інакше програма попросить.

## Neptunia Re;Birth1

Раніше переклад Neptunia йшов через Crowdin, stcm-editor і `nr1_packer`.
Тепер усе так само, як в інших іграх: книги Excel і кнопки 1–2–3. Нічого
перекодовувати вручну (preconvert) не треба, програма робить це сама.

- **Перший запуск «1».** Програмі потрібні **оригінальні** файли гри. Якщо
  в теці гри лежать архіви, які ти колись замінював вручну, програма
  скаже про це й зупиниться. Тоді в Steam: Neptunia Re;Birth1 →
  Властивості → Встановлені файли → **«Перевірити цілісність файлів
  гри»**, і натисни «1» ще раз. Якщо в теці гри лежить `dinput8.dll` зі
  старого патча, прибери його: він більше не потрібен.
- **Пиши звичайну українську**, з `і ї є ґ` і апострофом `'`. Програма
  сама запише літери так, як їх розуміє гра, і сама перебудує шрифти.
  Символів `« » — …` у шрифті гри немає, програма замінить їх на `" " - ...`.
- **Назви монстрів і персонажів** живуть у полях фіксованої довжини:
  **не більше 31 символу** (підказка в книзі це показує). Довша назва
  лишиться англійською, «Перевірити переклад» попередить заздалегідь.
- **Коди** `%s`, `%u`, `%04u`, `%%`, `#FontColor[%u]`, `#FontColorB`,
  `#Icon[%u]`, `<BLANK>`: переносити в переклад **як є**. Загублений код
  «Перевірити переклад» покаже.
- Імена мовців у сценах (колонка «Хто») беруться з книги «01 Меню та
  система», рядки `IDS_EVT_TITLE_NAME_SUB_…`. Там їх і перекладай.

## Коли в грі замість літер квадратики або літера зникла

Значить, у шрифті гри немає такого символу. Найчастіше це рідкісні знаки
(наприклад `ō`) — «Перевірити переклад» про них скаже заздалегідь. Якщо
трапилось щось інше, напиши програмісту; перекладати далі можна, просто
поки не видно результату.
